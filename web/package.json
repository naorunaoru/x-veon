{
  "name": "xtrans-demosaic-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd wasm && wasm-pack build --target web --release",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "onnxruntime-web": "^1.21.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-wasm": "^3.4.1"
  }
}
